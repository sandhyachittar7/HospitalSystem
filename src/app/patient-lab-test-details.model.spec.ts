import { PatientLabTestDetails } from './patient-lab-test-details.model';

describe('PatientLabTestDetails', () => {
  it('should create an instance', () => {
    expect(new PatientLabTestDetails()).toBeTruthy();
  });
});
